const TeamMember = require("./TeamMember");

class Intern extends TeamMember {
    constructor(school) {
            super(name, id, email);
            this.school = school;
        }
        //methods go here
};

module.exports = Intern